name: Hello World!

on:
  push: 
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
    steps:
      - name: Current date 
        run: |
          echo date
    steps:
      - name: Current dir
        run: | 
          echo ls -l
